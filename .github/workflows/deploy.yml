name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: web-linux/package-lock.json

      - name: Install dependencies
        working-directory: ./web-linux
        run: npm ci

      - name: TypeScript type check
        working-directory: ./web-linux
        run: npm run typecheck

      - name: Lint
        working-directory: ./web-linux
        run: npm run lint -- --max-warnings 50 || true

      - name: Build (GitHub Pages mode)
        working-directory: ./web-linux
        run: npm run build

      - name: Copy public assets to dist
        run: |
          cp ./web-linux/public/sw.js ./dist/sw.js
          cp ./web-linux/public/404.html ./dist/404.html
          cp ./web-linux/public/.nojekyll ./dist/.nojekyll
          cp ./web-linux/public/favicon.svg ./dist/favicon.svg
          cp ./web-linux/public/icons.svg ./dist/icons.svg
          cp ./web-linux/public/manifest.json ./dist/manifest.json

      - name: Verify build output
        run: |
          echo "--- Checking dist/index.html exists ---"
          test -f ./dist/index.html && echo "OK: dist/index.html exists" || (echo "FAIL: dist/index.html missing" && exit 1)
          echo "--- Checking dist/assets folder ---"
          test -d ./dist/assets && echo "OK: dist/assets/ exists" || (echo "FAIL: dist/assets/ missing" && exit 1)
          echo "--- Checking dist/favicon ---"
          test -f ./dist/favicon.svg && echo "OK: dist/favicon.svg exists" || echo "WARN: dist/favicon.svg missing"
          echo "--- Checking dist/manifest ---"
          test -f ./dist/manifest.json && echo "OK: dist/manifest.json exists" || echo "WARN: dist/manifest.json missing"
          echo "--- Checking dist/.nojekyll ---"
          test -f ./dist/.nojekyll && echo "OK: dist/.nojekyll exists" || echo "WARN: dist/.nojekyll missing"
          echo "--- Checking dist/sw.js ---"
          test -f ./dist/sw.js && echo "OK: dist/sw.js exists" || (echo "FAIL: dist/sw.js missing - PWA will not work" && exit 1)
          echo "--- Checking dist/404.html ---"
          test -f ./dist/404.html && echo "OK: dist/404.html exists" || echo "WARN: dist/404.html missing"
          echo ""
          echo "--- Deployment inventory ---"
          echo "Dist root files:"
          ls -1 ./dist/ | head -30
          echo ""
          echo "Asset files count:"
          ls ./dist/assets 2>/dev/null | wc -l || echo 0

      - name: Setup Pages
        uses: actions/configure-pages@v4
        with:
          static_site_generator: vite

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
