# Dependencies
node_modules/
web-linux/node_modules/

# Build output - These are deployed to GitHub Pages
# Note: root-level assets/, index.html, favicon.svg, icons.svg, .nojekyll,
# manifest.json, 404.html ARE tracked because they are the deployed site
# content (rebuilt locally and committed; the GitHub Actions workflow also
# rebuilds them on every push to main).
dist/
dogfood-output/

# Development
.env
.env.local
.env.*.local
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor directories
.DS_Store
.vscode/
.idea/
*.swp
*.swo
*~

# OS
Thumbs.db

# Temporary files
*.tmp
*.temp
.cache/
